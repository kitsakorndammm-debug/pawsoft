import { redirect } from 'next/navigation'

import { ROUTE_EMPLOYEES } from '@/lib/routes'

/**
 * **ไม่มีหน้าตารางบัญชีผู้ใช้ และตั้งใจไม่มี**
 *
 * บัญชีทั้งหมดจัดการผ่านกล่องที่เปิดจากแถวพนักงาน (`user-account-dialog.tsx`) —
 * เปิดบัญชี · แก้ชื่อผู้ใช้กับบทบาท · รีเซ็ตรหัส · ระงับ · ปลดล็อก
 *
 * เพราะคำถามที่คนถามจริงคือ "คนนี้เข้าระบบได้ไหม" ไม่ใช่ "บัญชีในระบบมีกี่บัญชี" ·
 * ตารางบัญชีแยกจะบังคับให้คนจำชื่อผู้ใช้เพื่อหาว่าเป็นของใคร ทั้งที่เขารู้จักชื่อคน ·
 * และ BE ของ PMK ก็ไม่มีเส้นที่คืนรายการบัญชีทั้งหมดโดยตั้งใจเช่นกัน
 *
 * เส้นนี้เหลือไว้เพราะเมนูเก่ากับลิงก์ที่คนบุ๊กมาร์กไว้อาจยังชี้มา — พาไปหน้าพนักงาน
 * แทนที่จะ 404
 */
export default function UsersPage() {
  redirect(ROUTE_EMPLOYEES)
}
