import { cn } from '@/lib/utils'

/**
 * บรรทัดบอกว่าข้อมูลในหน้านี้ไปโผล่ที่ไหน — **หนึ่งประโยค ใต้แท็บ เหนือตาราง**
 *
 * ทะเบียนหน้าหนึ่ง ๆ ตอบว่า "แถวนี้ชื่ออะไร" ได้อยู่แล้ว แต่ไม่ตอบว่า "แถวที่เพิ่งเพิ่ม
 * ไปโผล่ตรงไหนของงานจริง" · คนที่เปิดหน้ามาแก้ทะเบียนของฝ่ายผลิตมักไม่ใช่คนที่ยืนอยู่
 * หน้าสถานีนั้น และไม่มีอะไรบนหน้าจอบอกให้
 *
 * **ที่นี่ไม่ใช่ที่อธิบายว่าหน้านี้ใช้ยังไง** — ปุ่มกับตารางบอกไปแล้ว · ประโยคเดียว
 * ที่ควรอยู่ตรงนี้คือปลายทางของข้อมูล: สถานีไหน ใบไหน ขั้นตอนไหนที่หยิบไปใช้
 *
 * แยกเป็นชิ้นแทนที่จะพิมพ์ `<p className="text-xs text-muted-foreground">` ในทุกหน้า
 * เพราะระยะกับสีของมันควรเปลี่ยนพร้อมกันทั้งระบบ ไม่ใช่ทีละหน้าที่ใครแก้
 */
export function PageHint({ children, className }: { children: React.ReactNode; className?: string }) {
  return <p className={cn('text-xs text-muted-foreground', className)}>{children}</p>
}
