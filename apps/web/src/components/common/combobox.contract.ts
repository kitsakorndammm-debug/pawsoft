/**
 * ข้อความของ Combobox ที่โลกภายนอกผูกไว้ — **TS เปล่า ไม่มี React**.
 *
 * แยกออกมาเพราะฝั่งเทสต้องกดตัวเลือก "ไม่ระบุ" เพื่อพิสูจน์ว่าล้างค่า FK ได้จริง.
 * พิมพ์สตริงนั้นซ้ำในเทส = วันที่แก้คำในปุ่มแล้วเทสพังแบบ "หาไม่เจอ" แทนที่จะพังตอน compile.
 */
export const comboboxContract = {
  /** ตัวเลือกล้างค่า — มีเฉพาะช่องที่ไม่บังคับ */
  clearLabel: 'ไม่ระบุ',
} as const
